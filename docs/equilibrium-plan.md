# 從「精確 oracle」到「求解均衡」：GTO 一詞要怎麼才用得起

`tests/test_claims.py` 目前擋掉 `gto`、`理論最佳`、`最佳解` 等字。擋的理由不是這些詞本身不能用，
而是**目前還不成立**。這份筆記記錄：差在哪、什麼東西已經到位、以及要補什麼才能講一句有範圍且為真的話。

## 現在缺的是什麼

差距不在算力，而在**沒有任何一家在求最佳反應**。

- 全專案沒有 best response / equilibrium / Nash / CFR / exploitability 的實作
  （`grep -rniE "best.?response|equilibri|nash|cfr|exploitab" taimahjong/ server/` 無結果）。
- `taimahjong/reference_ev.py` 是**精確的終局／結算 oracle**，不是賽局求解器。它自己的 docstring
  講得很清楚：窮舉每一種實際抽牌順序，但**套用單一決定性出牌 policy**，因此它認證的是終局分類、
  結算與 aggregation 機制，不是對手模型的真實性。
- production rollout 的其他三家跑固定 heuristic policy，不會針對你的打法調整。

GTO 要的是一組策略組合，其中沒有任何一家能靠偏離獲利。這個 repo 目前**連「偏離能賺多少」都還沒量**。

## 已經到位、可以直接接手的東西

淺牌牆殘局這一塊的地基其實鋪得比想像中好：

| 已有 | 位置 | 為什麼對求解有用 |
| --- | --- | --- |
| 對抽牌順序的窮舉分支 | `reference_ev.py` 的 `branch()` | 值函數不需要抽樣，直接精確 |
| 精確有理數機率 | `branch(..., Fraction(1))` | 沒有蒙地卡羅誤差，均衡判定不會被雜訊污染 |
| 四家暗手全可見的狀態表示 | `ReferenceState` | 完全資訊子賽局，可以直接做動態規劃 |
| 榮和判定與四家零和結算 | `_ron_claims`、`_settlement` | 終局收益已經是正確的，不必重寫 |
| 26 個分層抽樣的驗收語料 | `MIN_GATE_CASES = 26` | 現成的測試床 |

限制條件也很明確：牌牆最多 4 張、`reference_ev.py:303` 直接 `assert melds_declared == 0`
（子賽局內不含吃碰槓）。

## 三個步驟，以及各自能換到什麼說法

**步驟 1 — 讓 policy 變成自由變數。**
現在出牌 policy 是注入的常數。改成可以對 acting seat 窮舉所有合法策略。狀態空間在 ≤4 張牌牆下
小到可以窮盡，且值函數已經是精確的。
*換到的說法：* 還沒有，這是基礎建設。

**步驟 2 — 算 best response，量 exploitability。**
固定其他三家的現行 policy，對 acting seat 求最佳反應，然後量 production policy 跟它差多少。
*換到的說法：* 「production policy 在 ≤4 張牌牆殘局中的 exploitability 為 X 台」。
**這一步的投報率最高**——它產出一個數字，回答「我的策略被針對能被賺走多少」，而且不需要真的解出
均衡。對量化職缺來說，這個數字比「GTO」三個字有說服力得多。

**步驟 3 — 迭代到近似均衡。**
反覆做 best response 直到收斂，在該受限子賽局內得到均衡策略組合。
*換到的說法：* 「在 ≤N 張牌牆、無鳴牌的殘局子賽局中求解到均衡」——**要連範圍一起講**。

## 就算做完，仍然不能說什麼

- 子賽局 ≠ 完整牌局。中盤 `ev_rank` 可以評估到 24 巡，跟 4 張牌牆的殘局不是同一個問題。
- 不含鳴牌的均衡，不等於含吃碰槓的均衡。
- 均衡策略與**真人**對手無關；bot-domain calibration 的限制照舊。

換句話說，步驟 3 之後可以講的是「在這個明確定義的子賽局裡解出了均衡」，而不是「這是一個 GTO
訓練器」。範圍講清楚的小主張，遠比講不清楚的大主張值錢。

## 解除條件

`tests/test_claims.py` 的守門條件應該逐項解除，不是整包拿掉：真的解出哪個子賽局，就只放行對應
範圍的措辭。

---

## 步驟 2 已完成（2026-08-24）：exploitability 量出來了

`taimahjong/best_response.py` 實作了 acting seat 的 best response 與
exploitability 量測，語料沿用 `reference_ev` 那 26 個 ≤4 張牌牆局面。

### 可以講的一句話

> production rollout policy 在 ≤4 張牌牆、無鳴牌的殘局子賽局中，在 production
> 自身的對手信念模型下，exploitability 為 **0.556 台**（26 局面平均，sims=60），
> clairvoyant 上界 **0.898 台**。26 個局面中有 11 個恰為 0。

範圍要連著講：這不是中盤（`ev_rank` 評到 24 巡）、不含吃碰槓、不是對真人。

### 設計上的兩個決定

**對照組**：acting seat 與三家對手都跑 `_production_discard_policy`，best response
只替換 acting seat。這是 production rollout 實際模擬的組合。

**資訊限制**：兩邊吃同樣的資訊。production 的 rollout policy 平常拿到的是 rollout
的真實剩餘牌牆——那對真人是暗的——所以在這裡 acting seat 自己的決策改吃它的信念池
（4 − 手牌 − 可見 − 已觀察到的棄牌）。少了這一步，被量測的 policy 會看得比 best
response 多，非負不變量就失去意義。

### 一個必須記錄的負面結果：完整 information-set BR 會退化

原計畫的「對整個 policy 求 information-set best response」**在任何負擔得起的樣本數
下都會退化成 clairvoyant**，原因是結構性的而非實作錯誤。

acting seat 在第二次決策時觀測到自己摸的那張加三家各棄的一張，這個觀測太細，兩個
抽樣世界幾乎不可能落進同一個資訊集：

| sims | 資訊集 | 被 >1 個世界抵達 | full 值 | clairvoyant 值 |
|---:|---:|---:|---:|---:|
| 20 | 304 | 0 | 1.762 | 1.762 |
| 100 | 1,631 | 3 | 1.675 | 1.675 |
| 200 | 3,170 | 17 (0.5%) | 1.522 | 1.522 |

資訊集數隨樣本數線性成長，所以加樣本只會讓退化更嚴重，不會改善。這個模式以
`mode="full"` 保留，並由 `test_full_mode_information_sets_are_effectively_singletons`
釘住其退化，避免下一個人把它的數字當成 exploitability。

**繞過的方式**：只優化開局那次決策（`mode="opening"`，預設）。所有抽樣世界依定義
共享開局資訊集，所以沒有退化，而且那正是 `ev_rank` 存在要回答的決策。續打維持
measured policy，兩邊一致。

### 這個數字怎麼用

policy 偏誤 0.56–0.90 台，與 `docs/hidden-world-strata.md` 量到的抽樣雜訊
（sims=2000 時 paired gap 半寬 0.67 台）**同一量級**。沒有一方輾壓另一方，因此：

- 加樣本仍有意義，雜訊還沒被偏誤淹沒；
- 但總誤差有一個約 0.6 台的地板是加樣本跨不過去的，把雜訊壓到遠低於此純屬拋光。

**這個比較要打折**：exploitability 語料是 ≤4 張牌牆殘局，strata 研究是中盤最多 12
巡，兩個不同 regime，屬量級參考而非嚴格對照。

### 解除了哪一條守門

`tests/test_claims.py` 的 `gto`／`理論最佳`／`最佳解` **維持全部封鎖**。本步驟量的是
exploitability，不是均衡；步驟 3（迭代到近似均衡）尚未開始。

---

## 步驟 3（2026-08-24）：受限策略集上的經驗賽局，**沒有**解出均衡

`taimahjong/empirical_game.py`。先講結論：**均衡沒解出來**，但語料把 16 個候選 profile
中的 12 個排除掉了，而且 production 的 rollout policy 被證明**不是**均衡。

### 原計畫的第 3 步不可行

「反覆對四家做 best response 直到收斂」在**無限制策略空間**下做不到，理由與步驟 2 的
負面結果完全相同：非行動家要摸完牌才決策，它的資訊集以「該世界的手牌」為 key，在抽樣
世界之間幾乎必然是單例。只有行動家的**開局**決策在所有世界共享資訊集，而那只有一家。

### 改成什麼

把策略空間限制成**明列的有限整體 policy 集合**，在那個集合上建經驗賽局。這與 poker
solver 把連續下注尺寸抽象成幾個尺寸是同一個動作：均衡在抽象內精確，抽象外不代表任何
事情。**每一句基於這個模組的話都必須把策略集一起講。**

| 策略 | 行為 |
| --- | --- |
| `efficiency` | production 的 `_production_discard_policy`（最大化打完後的進張） |
| `safety` | 優先切「未見剩餘為 0」的牌，同安全度內回落效率 |
| `oracle` | `reference_ev._policy_discard` — **量測後排除於預設集之外**，見下 |

每個 profile 的每家收益是**單一世界內對抽牌順序的精確 `Fraction` 期望**；暗手抽樣，
所有 profile 共用同一批世界（CRN，profile 之間成對比較）。每家只吃自己的信念池。

### `oracle` 是同一個行為，而且貴 12 倍

26 個局面 × 20 世界共 **5,415 個可達決策上分歧 0 次**。成本卻是：

| profile | ms／世界 |
|---|---:|
| 全 `oracle` | 143.1 |
| 全 `efficiency` | 11.5 |
| 全 `safety` | 3.5 |

81 個 profile 中有 65 個含 `oracle`，每一個都在重算已經有的答案。改用
`DEFAULT_STRATEGIES = ("efficiency", "safety")` 的 16 個 profile 後，**sims 從 20 提到
400，成本反而從 2,462s 降到 833s**。`oracle` 保留可用但不預設，
`test_efficiency_and_oracle_are_one_behaviour_not_two` 會在它哪天真的分歧時失敗。

### 為什麼點估計不能報

sims=20 的完整表給出 regret **+0.293 台**、唯一均衡 `SESE`，看起來像個結果。但那個
「均衡」在其他預算下完全不重現：

| sims | 點估計均衡 |
|---:|---|
| 20 | `SESE` |
| 40 | `SESS` |
| 60 | 無 |
| 400 | `SEES` |

`equilibria()` 只比較點估計，預算薄時它回報的是**雜訊剛好偏好的那個 profile**。
（baseline regret 的**正負號**倒是每個預算都撐住了，撐不住的是均衡的身分。）

所以 `EmpiricalGame` 改成保留每個 (case, world) 的收益向量，並提供
`regret_interval()`，兩件事：

1. **最大值在每次 resample 內部取**。先挑出「看起來最好的那一家」再對它做 bootstrap，
   會給出一個由同一批雜訊選出來的座位的區間。
2. **重抽的單位是局面，不是世界**。同一局面的 400 個世界共用那手牌，不是獨立樣本；
   `docs/hidden-world-strata.md` 早就把局面當叢集。實測差別（同一個 +0.265 點估計）：
   重抽世界 [+0.189, +0.340]，重抽局面 [+0.082, +0.519]。**寬的那個才是誠實的**，因為
   實際只看過 26 手牌。

`equilibria()` 保留為點估計版，docstring 標明必須配區間讀。

### 主結果（26 局面 × 400 世界 = 10,400 單位，833s）

零和殘差 3.8e-12。16 個 profile 的 regret 與 95% CI（叢集 bootstrap，重抽局面）：

| profile | regret | 95% CI | 判定 |
|---|---:|---|---|
| `EEEE` | +0.2647 | [+0.082, +0.519] | **不是均衡** |
| `EEES` | +0.2417 | [+0.041, +0.493] | 不是均衡 |
| `EESE` | +0.2399 | [+0.080, +0.493] | 不是均衡 |
| `EESS` | +0.2058 | [+0.051, +0.461] | 不是均衡 |
| `ESEE` | +0.2666 | [+0.069, +0.523] | 不是均衡 |
| `ESES` | +0.2276 | [+0.042, +0.477] | 不是均衡 |
| `ESSE` | +0.2329 | [+0.081, +0.493] | 不是均衡 |
| `ESSS` | +0.1856 | [+0.057, +0.439] | 不是均衡 |
| `SEEE` | +0.0721 | [+0.002, +0.203] | 不是均衡 |
| `SEES` | −0.0021 | [−0.072, +0.189] | **未判定** |
| `SESE` | +0.0619 | [+0.027, +0.197] | 不是均衡 |
| `SESS` | +0.0454 | [−0.026, +0.183] | **未判定** |
| `SSEE` | +0.0329 | [−0.019, +0.189] | **未判定** |
| `SSES` | +0.0021 | [−0.051, +0.177] | **未判定** |
| `SSSE` | +0.0765 | [+0.025, +0.225] | 不是均衡 |
| `SSSS` | +0.0722 | [−0.026, +0.226] | **未判定** |

（`E` = efficiency，`S` = safety，位置依座位 0–3。）

### 可以講的兩句話

> 在 ≤4 張牌牆、無鳴牌的殘局子賽局中，於 {efficiency, safety} 這個兩策略抽象下，
> production 的 rollout policy 組合（全 efficiency）**不是**均衡：單邊偏離最多可多賺
> **0.265 台**（95% CI [+0.082, +0.519]，26 局面 × 400 世界，重抽局面）。

> 同一份量測**沒有**解出均衡。16 個 profile 中 11 個被排除，剩下 5 個
> （`SEES`、`SESS`、`SSEE`、`SSES`、`SSSS`）在這個預算下分不開。

### 漏洞的形狀：位置相依，而 production policy 看不到位置

把「某一家改吃 safety」的收益按**相對摸牌順位**（0 = 行動家本人）拆開，同樣以局面為
叢集：

| 相對順位 | 收益 | 95% CI | 判定 |
|---:|---:|---|---|
| 0（行動家） | +0.2590 | [−0.008, +0.561] | 未判定（差一點） |
| 1（下家） | −0.1518 | [−0.305, −0.000] | **防守反而虧** |
| 2 | +0.0066 | [−0.081, +0.094] | 未判定 |
| 3（最後摸） | +0.1251 | [+0.041, +0.212] | **防守有賺** |

> **2026-08-25 更正**：順位 1 那一列的「防守反而虧」**站不住**。上界 −0.000 是壓在零上，
> 換一個 bootstrap 種子就會跨過去（八個種子的上界落在 −0.002…+0.018）。正確判定是
> **未判定**。下一節以角色重建後重測了同一件事，結論相同：全語料未判定，只有切到牌牆
> 4 張那格才穩健為負。

順位 1 再按牌牆深度切，牌牆 4 張那格有 18 個局面（其餘各格只有 2–4 個局面，叢集數
太少，不列）：

| 相對順位 | 牌牆 | 局面數 | 收益 | 95% CI |
|---:|---:|---:|---:|---|
| 1 | 4 | 18 | −0.2426 | [−0.430, −0.055] |

所以**不是「多防守就對」**：最後一個摸牌的人防守有賺，但行動家的下家在還有 4 張可摸時
防守要虧 0.24 台。正確行為取決於**相對順位**（很可能也取決於離終局多遠，但每個牌牆深度
只有 2–4 個局面，這份語料切不動），而 `_production_discard_policy` 是座位無關、深度無關
的純進張規則。這是這一步指出的可修改缺口。

行動家本人該不該防守（+0.259）在叢集層級**差一點就跨過零**，26 個局面還撐不起這句話。
要坐實它需要更多局面，不是更多世界。

### 機制正確性的兩個內部檢查

- **恰好零**：牌牆只剩 `w` 張時，只有相對順位 1..`w` 的座位輪得到摸牌；順位超過 `w`
  的座位一張都不會打出去，改它的策略對任何一家的收益影響必須是**恰好** 0。4,000 個
  這類 unit 觀測的 `max |gain|` 是 `0.0`——不是很小，是零。任何把隱藏狀態或錯誤座位
  串進來的 bug 都做不出恰好零。由
  `test_a_seat_the_wall_never_reaches_cannot_change_anything` 釘住。
- **零和殘差 3.8e-12**：四家結算只搬動價值、不創造價值。單一世界版本以有理數斷言
  `sum(payoffs) == 0` **精確相等**（`test_one_world_is_exact_and_zero_sum`）。

### 這一步還是不能講什麼

- **沒有解出均衡**，所以「在殘局子賽局中求解到均衡」這句話**仍然不能講**。
- 抽象只有兩個行為。換一個策略集就是另一個賽局；`safety` 只是很粗的現物近似。
- 賽局的「玩家」是**絕對座位編號**，但語料裡行動家 13 局在座位 0、13 局在座位 1，
  所以同一個座位編號在不同局面扮演不同角色。上面的順位拆解是繞過這點的做法；更乾淨
  的做法是直接以角色（行動家／各相對順位）為玩家重建賽局，尚未做。
- 子賽局 ≠ 完整牌局，不含鳴牌，不對真人。

### 解除了哪一條守門

**一條都沒有。** `gto`／`理論最佳`／`最佳解` 維持封鎖。本步驟證明的是「production
不是均衡」，那是個否定句；解除條件要的是肯定句——真的解出某個子賽局的均衡。

### 要把剩下 5 個 profile 分開，該加什麼

量測本身指出了瓶頸：**解析度受限於局面數（26），不是世界數（400）**。叢集 bootstrap
重抽的是局面，所以世界再加也只是把每個局面的均值磨得更準，26 個叢集的離散度不會變。

按投報率排序：

1. ~~**以角色重建賽局**~~ — **已做，見下一節。括號裡的理由是錯的**：四個座位每一個
   都是「兩種角色各 13 局」的混合，沒有哪個座位只出現在一種角色。重建**不會多出任何
   觀測**，換到的是語意乾淨，不是解析度。
2. **加局面**。`representative_reference_cases()` 是 26 個分層抽樣局面；叢集層級的
   CI 半寬約 ∝ 1/√(局面數)，要把 ±0.22 壓到 ±0.10 需要約 5 倍局面數。牌牆深度的
   拆解（現在每格只有 2–4 個局面）也只有這條路能救。
3. **把 `safety` 換成真的放銃機率估計**。現在的「未見剩餘為 0」是很粗的現物近似；
   抽象裡的策略越像真正的候選打法，均衡才越有參考價值。

---

## 步驟 3b（2026-08-25）：玩家改成角色，**還是沒有**解出均衡

`taimahjong/empirical_game.py` 的 profile 從「絕對座位編號」改成「角色」：索引 0 是
行動家，1／2／3 是它之後第 1／2／3 個摸牌的座位。座位↔角色的旋轉只在
`profile_payoffs` 內部一個地方發生（`profile[(seat - acting) % 4]`，回傳值再轉回角色
順序）。CRN、有理數列舉、信念池限制、叢集 bootstrap 重抽局面，全部沒動。

先講結論：**均衡還是沒解出來。** 16 個 profile 裡穩健排除掉 12 個（座位版是 11 個），
剩 4 個分不開，沒有任何一個的 CI 落在 0 以下。

### 這一步沒有多拿到觀測

計畫書原本寫「每個局面對每個角色都貢獻一個觀測（現在座位 2、3 各只在一種角色出現）」，
那是錯的。語料 26 局面裡行動家 13 局在座位 0、13 局在座位 1，所以：

| | acting=0（13 局） | acting=1（13 局） |
|---|---|---|
| 座位 0 | 角色 0 | 角色 3 |
| 座位 1 | 角色 1 | 角色 0 |
| 座位 2 | 角色 2 | 角色 1 |
| 座位 3 | 角色 3 | 角色 2 |

**四個座位每一個都是兩種角色各 13 局的混合。** 重建前後每個玩家都是 26 個局面叢集
× 400 世界，總共 94 個「真的會摸到牌」的 (局面, 玩家) 對只是重新分組，一個都沒多。

真正換到的是**同質性**：座位版的 profile 第 0 位在 13 局代表「行動家改防守」、另 13 局
代表「順位 3 改防守」，一格裡平均了兩件不同的事；角色版的第 0 位 26 局都是行動家。

**代價要一起記**：莊家是絕對座位 0（`is_dealer = seat == 0`）。座位版在莊家維度是乾淨
的、角色維度是髒的；角色版剛好相反，每個角色現在混了 13 局莊、13 局閒。這是**把混淆
搬家，不是消除混淆**。

### 主結果（26 局面 × 400 世界，739s）

零和殘差 1.1e-16。95% CI 為叢集 bootstrap 重抽局面，bootstrap 種子 7；「seed 穩健」
欄記錄換 8 個 bootstrap 種子後判定是否不變。

| profile | regret | 95% CI | 判定 | seed 穩健 |
|---|---:|---|---|---|
| `EEEE` | +0.2590 | [+0.100, +0.555] | **不是均衡** | 是 |
| `EEES` | +0.2680 | [+0.056, +0.557] | 不是均衡 | 是 |
| `EESE` | +0.2432 | [+0.064, +0.540] | 不是均衡 | 是 |
| `EESS` | +0.2446 | [+0.012, +0.532] | **未判定** | 否（下界 −0.003…+0.015） |
| `ESEE` | +0.2352 | [+0.121, +0.533] | 不是均衡 | 是 |
| `ESES` | +0.2218 | [+0.094, +0.515] | 不是均衡 | 是 |
| `ESSE` | +0.2038 | [+0.091, +0.503] | 不是均衡 | 是 |
| `ESSS` | +0.1852 | [+0.071, +0.482] | 不是均衡 | 是 |
| `SEEE` | +0.1317 | [+0.044, +0.229] | 不是均衡 | 是 |
| `SEES` | +0.0387 | [−0.030, +0.119] | **未判定** | 是 |
| `SESE` | +0.0722 | [+0.021, +0.143] | 不是均衡 | 是 |
| `SESS` | −0.0387 | [−0.080, +0.051] | **未判定** | 是 |
| `SSEE` | +0.1563 | [+0.073, +0.312] | 不是均衡 | 是 |
| `SSES` | +0.1277 | [+0.024, +0.273] | 不是均衡 | 是 |
| `SSSE` | +0.1483 | [+0.053, +0.303] | 不是均衡 | 是 |
| `SSSS` | +0.1283 | [−0.020, +0.278] | **未判定** | 是 |

（`E` = efficiency，`S` = safety，位置依角色 0–3，0 = 行動家。）

`EESS` 的下界壓在零上，換種子就翻面，所以照最保守的讀法算**未判定**——穩健排除的是
12 個，不是 13 個。

### 可以講的兩句話

> 在 ≤4 張牌牆、無鳴牌的殘局子賽局中，於 {efficiency, safety} 這個兩策略抽象下、以
> **角色**（行動家／相對順位 1–3）為玩家，production 的 rollout policy 組合（全
> efficiency）**不是**均衡：**行動家**單邊改吃 safety 可多賺 **0.259 台**
> （95% CI [+0.100, +0.555]，26 局面 × 400 世界，重抽局面）。

> 同一份量測**沒有**解出均衡。16 個 profile 中 12 個被穩健排除，剩下 4 個
> （`EESS`、`SEES`、`SESS`、`SSSS`）在這個預算下分不開，沒有任何一個的 CI 落在 0 以下。

比座位版多能講的是「**行動家**」三個字：座位版報的是「座位 0」，而座位 0 只有一半語料
是行動家。

### 一個變好、但仍然只是點估計的東西

點估計均衡（`equilibria()`）在座位版隨預算亂跳（20→`SESE`、40→`SESS`、60→無、
400→`SEES`）；角色版在 20／100／400 三個預算都是同一個 `SESS`。這是抽象裡唯一一個
每個預算都同意的候選，而且它的形狀跟下面的角色偏離表一致（行動家防守、順位 1 不防守、
順位 3 防守）。**但它的 CI [−0.080, +0.051] 跨零，所以它不是「證明是均衡」，是「還沒
排除掉」。** 不准把這句話說成解出來了。

### 角色偏離表（現在是原生量的，不是事後拆的）

從全 efficiency 出發，單一角色改吃 safety：

| 角色 | 收益 | 95% CI | 判定 | seed 穩健 |
|---|---:|---|---|---|
| 0（行動家） | +0.2590 | [+0.004, +0.555] | **未判定** | 否（下界 −0.016…+0.004） |
| 1（下家） | −0.1518 | [−0.303, +0.008] | **未判定** | 否（上界 −0.002…+0.018） |
| 2 | +0.0066 | [−0.086, +0.098] | 未判定 | — |
| 3（最後摸） | +0.1251 | [+0.040, +0.210] | **防守有賺** | 是 |

點估計跟步驟 3 的事後拆解完全相同（+0.2590／−0.1518／+0.0066／+0.1251），這是預期的
——事後拆解在算術上就是同一件事。**變了的是判定**：角色 0 和角色 1 兩格都壓在零上，
換 bootstrap 種子就翻面，兩格都得算未判定。步驟 3 把角色 1 寫成「防守反而虧」是超讀，
已在上面更正。

全語料唯一撐得住的是**角色 3 防守有賺**。再切到牌牆 4 張那格（18 個局面，唯一叢集數
夠的一格）：

| 角色 | 牌牆 | 局面數 | 收益 | 95% CI | 判定 |
|---:|---:|---:|---:|---|---|
| 0 | 4 | 18 | +0.0566 | [−0.185, +0.305] | 未判定 |
| 1 | 4 | 18 | −0.2426 | [−0.430, −0.058] | **防守虧** |
| 2 | 4 | 18 | +0.0186 | [−0.110, +0.147] | 未判定 |
| 3 | 4 | 18 | +0.1944 | [+0.085, +0.300] | **防守有賺** |

這一格的兩個判定換 8 個種子都不翻。所以「**位置相依**」這個缺口仍然成立，而且形狀比
步驟 3 講的更受限：**在牌牆還有 4 張時，行動家的下家防守要虧 0.24 台，最後摸的那家防守
賺 0.19 台**；全語料層級只有角色 3 那句撐得住。順帶一提，行動家自己那 +0.259 主要來自
淺牌牆局面——牌牆 4 張時只剩 +0.057 且未判定。

`_production_discard_policy` 是座位無關、深度無關的純進張規則，看不到這個特徵。這是這
一步指出的可修改缺口。

### 機制檢查

- **恰好零**維持成立，而且改成用角色陳述之後**才真的擋得住旋轉錯誤**：兩個「行動家坐
  座位 1、牌牆 2 張」的局面裡，閒置的是角色 3；若把 profile 當座位讀，第 3 格指到的是
  一個摸得到牌的座位，它的 tilt 在 8 個抽樣世界裡有 2 個會動到收益。所以
  `test_a_role_the_wall_never_reaches_cannot_change_anything` 不是空過。
- **單一世界零和**照舊以有理數精確斷言。
- 全套 `tests/test_empirical_game.py`：10 passed（快）＋ 2 passed（slow，139s）。

### 這一步還是不能講什麼

- **沒有解出均衡**，「在殘局子賽局中求解到均衡」**仍然不能講**。
- 角色版把混淆從角色搬到莊家，沒有消除。
- 抽象仍然只有兩個行為；子賽局 ≠ 完整牌局；不含鳴牌；不對真人。

### 解除了哪一條守門

**一條都沒有。** `gto`／`理論最佳`／`最佳解` 維持封鎖，`tests/test_claims.py` 未動。

### 還剩什麼

清單第 1 項做完了，而且證實它**不會**改善解析度。瓶頸原封不動地留在原地：

2. **加局面**（唯一能動 26 個叢集的路）。
3. **把 `safety` 換成真的放銃機率估計**。

---

## 步驟 3c（2026-08-26）：局面加倍到 52，**還是沒有**解出均衡

清單第 2 項做完了。先驗證「加局面會變窄」（`scripts/case_count_scaling.py`），再照
驗證結果建 52 個局面的專用語料（`taimahjong/empirical_corpus.py`），重出 16 個
profile 的表（`scripts/empirical_game_table.py`）。

### 先講結論

**均衡還是沒解出來。** 兩個世界種子下都是 12 個 profile 被穩健排除、4 個分不開，
**沒有任何一個 profile 的 CI 落在 0 以下**。分不開的那四個換了一組：步驟 3b 是
`EESS`／`SEES`／`SESS`／`SSSS`，現在是 `EEES`／`SEES`／`SESS`／`SSES`。

新拿到的是**角色 1 的判定**：從全 efficiency 出發，**行動家的下家改吃 safety 會虧
0.17–0.20 台**，全語料層級、兩個世界種子、八個 bootstrap 種子都不翻面。步驟 3b 只
有牌牆 4 張那一格撐得住這件事。

### 先做的驗證：加局面真的會變窄嗎

`scripts/case_count_scaling.py` 對現有 26 個局面做子抽樣（8／13／20／26 個局面
× 20／100／400 個世界），量 regret 的 CI 半寬。世界是每個局面各自 seed 的 IID 抽樣，
所以子抽樣在兩個軸上都合法；腳本開頭會先驗證自己算出的滿語料半寬與
`regret_interval()` 逐位元相同，不同就中止。

量到兩件事：

**一、統計量不能用中位數。** 26 個局面裡有 1 個吃掉 42–78% 的變異數，所以「k 個局面
的半寬中位數」量到的其實是「典型子集有沒有抽中那個局面」（機率 = k/26），會讓半寬看
起來**隨局面數變大**。改用子集平均（＝新語料的期望半寬）才有意義。

**二、局面軸每一格都比世界軸陡。** 半寬對局面數的擬合指數（−0.5 ＝ 剛好 1/√局面數，
26 局面 × 400 世界，兩個世界種子）：

| profile | 局面指數 | 世界指數 | 單一局面佔離散度 | 世界噪音佔離散度 |
|---|---:|---:|---:|---:|
| `EEEE` | −0.385／−0.392 | −0.122／−0.203 | 25%／23% | 8%／10% |
| `SESS` | −0.670／−0.693 | −0.357／−0.377 | 32%／15% | 39%／49% |
| `SEES` | −0.554／−0.630 | −0.316／−0.360 | 32%／15% | 39%／49% |
| `SSSS` | −0.333／−0.327 | −0.135／−0.174 | 25%／37% | 13%／16% |

擋住判定的 `SESS`／`SEES` 是 −0.55～−0.69，比 1/√局面數還好；`EEEE`／`SSSS` 是
−0.33～−0.39，比 1/√局面數差，因為它們的離散度有四分之一以上壓在單一局面上。所以
計畫書把「加局面」排在「加世界」前面是對的，**但只有對 `SESS`／`SEES` 那兩格值得做**。

事後對照：`EEEE` 的半寬從 26 局面的 0.247 縮到 52 局面的 0.167（預測 0.193），
`SEES` 從 0.106 縮到 0.055（預測 0.075），`SESS` 從 0.101 縮到 0.083（預測 0.071），
`SSSS` 從 0.162 縮到 0.158（預測 0.129）。四格裡兩格比預測好、兩格比預測差，平均縮到
0.75 倍，跟 1/√2 = 0.71 的預測大致相符。**但語料的深度組合也一起換了，所以這是參考
性對照，不是同一個量的前後比較。**

### 新語料：52 個局面，另開一份

`taimahjong/empirical_corpus.py` 的 `empirical_game_cases()`。
**`representative_reference_cases()` 和 `MIN_GATE_CASES = 26` 一個字都沒動**，四個
吃那個門檻的測試模組不受影響。

分開的技術理由：`best_response.observation_for()` 只從局面讀出行動家的 17 張手牌、
牌牆**長度**、座位、莊家連莊、計分制、以及哪幾家已宣告——就這些。`build_game` 之後
會從 production 的信念模型重抽暗牌與牌牆，所以**語料裡對手的手牌內容經驗賽局從來
沒讀過**，只是為了讓 `ReferenceState` 的驗證（每家 16 張、每種牌至多 4 張）通過而
機械填出來的。同一批局面拿去餵 reference oracle 會是錯的——oracle 真的會讀對手手牌。
這就是兩份語料不能合併的原因。

語料形狀是 **13 個手牌樣板 × 4 個牌牆深度 = 52**：

- 每個樣板在四個深度各出現一次，所以**牌牆深度不再跟手牌混淆**。步驟 3b 的 26 個
  局面有 18 個在深度 4、深度 2 和 3 各只有 2 個，那張深度表只有深度 4 那列讀得動。
- 牌牆深度決定哪些角色**存在**：行動家打完之後剩下的 d 次摸牌依序給角色 1、2、3、1。
  深度 1 只有角色 1 摸得到，角色 2、3 的偏離收益**恰好是 0**；深度 2 時角色 3 恰好
  是 0。平均分配等於把四分之一的語料花在角色 3 動不了的局面上——這是選平均分配要
  一起付的代價。
- 座位／連莊／計分制／威脅家數由 `_FACTORS` 指派，這張表搜過一次求邊際完全平衡：
  行動座位 26／26、連莊 26／26、計分制 26／26、威脅家數 13／13／13／13。
- 13 個樣板裡 5 個沿用驗收語料的手牌（逐張相同），8 個是均勻抽樣後只保留落在目標
  向聽數的手。**後者不是真實對局打到中晚期會出現的牌型分布**：均勻抽出來的 2 向聽
  手比實戰的孤張多。任何從這份語料出來的說法都要帶著這句。

`tests/test_empirical_corpus.py` 釘的是語料的**形狀**（平衡、深度、向聽分層、對手手牌
不被讀到、摸不到牌的角色恰好零），不是結論；結論仍然釘在 26 局面的
`tests/test_empirical_game.py` 上。

### 主結果（52 局面 × 100 世界，195s／196s）

零和殘差 1.9e-16／2.2e-16。95% CI 為叢集 bootstrap 重抽局面，8 個 bootstrap 種子
（7／8／11／13／17／19／23／29）全部確認過，任何一個種子翻面就降級為未判定。
下表是世界種子 1；括號內為世界種子 2。

| profile | regret | 95% CI | 判定 |
|---|---:|---|---|
| `EEEE` | +0.1521 (+0.1337) | [+0.022, +0.355] ([+0.014, +0.319]) | **不是均衡** |
| `EEES` | +0.1345 (+0.1156) | [−0.013, +0.335] ([−0.027, +0.299]) | **未判定** |
| `EESE` | +0.0950 (+0.0804) | [+0.029, +0.308] ([+0.035, +0.285]) | 不是均衡 |
| `EESS` | +0.0852 (+0.0610) | [+0.003, +0.303] ([+0.013, +0.269]) | 不是均衡 |
| `ESEE` | +0.1728 (+0.1985) | [+0.067, +0.313] ([+0.088, +0.346]) | 不是均衡 |
| `ESES` | +0.1518 (+0.1718) | [+0.060, +0.290] ([+0.070, +0.318]) | 不是均衡 |
| `ESSE` | +0.1572 (+0.1798) | [+0.078, +0.297] ([+0.085, +0.320]) | 不是均衡 |
| `ESSS` | +0.1290 (+0.1537) | [+0.053, +0.268] ([+0.064, +0.295]) | 不是均衡 |
| `SEEE` | +0.0592 (+0.0779) | [+0.002, +0.128] ([+0.037, +0.128]) | 不是均衡 |
| `SEES` | +0.0065 (−0.0277) | [−0.040, +0.070] ([−0.073, +0.074]) | **未判定** |
| `SESE` | +0.0308 (+0.0710) | [+0.009, +0.121] ([+0.023, +0.148]) | 不是均衡 |
| `SESS` | −0.0065 (+0.0277) | [−0.033, +0.133] ([−0.020, +0.168]) | **未判定** |
| `SSEE` | +0.1062 (+0.1726) | [+0.026, +0.223] ([+0.036, +0.305]) | 不是均衡 |
| `SSES` | +0.0770 (+0.1397) | [−0.004, +0.226] ([+0.011, +0.261]) | **未判定**（世界種子 2 排除） |
| `SSSE` | +0.0850 (+0.1705) | [+0.052, +0.327] ([+0.053, +0.305]) | 不是均衡 |
| `SSSS` | +0.0481 (+0.1400) | [+0.027, +0.343] ([+0.028, +0.282]) | 不是均衡 |

（`E` = efficiency，`S` = safety，位置依角色 0–3，0 = 行動家。）

### 判準本身的問題：這個規則永遠證不出均衡

「CI 完全落在 0 以下才叫證明是均衡」這條規則**在真正的均衡上必然失敗**。均衡的定義
就是最大偏離收益恰好等於 0，而一個涵蓋 0 的量的 95% CI 永遠會跨 0。所以這條規則只能
用來**排除**，不能用來**證明**；`SESS` 的最大偏離收益現在是 −0.0065 台，離 0 比量測
精度還近，再加多少局面都不會讓它的上界掉到 0 以下。

能講而且講得出來的是 **ε-均衡**：用 regret 的 CI 上界當 ε。目前最緊的是 `SEES`，
上界 +0.070／+0.074 台，也就是「在這個抽象裡，`SEES` 至多是一個 **0.07 台的
ε-均衡**」。這個說法會隨預算變緊，而「證明是均衡」不會。

### 角色偏離表（從全 efficiency 出發，單一角色改吃 safety）

這張表不做 max 選擇，所以是單一偏離的直接 bootstrap，**與上表的 regret 欄不可比**。

| 角色 | 收益 | 95% CI 世界種子1 | 世界種子2 | 判定 |
|---|---:|---|---|---|
| 0（行動家） | +0.152 (+0.134) | [−0.037, +0.355] | [−0.044, +0.319] | 未判定 |
| 1（下家） | −0.173 (−0.199) | [−0.302, −0.044] | [−0.344, −0.048] | **防守會虧** |
| 2 | −0.074 (−0.076) | [−0.149, −0.008] | [−0.181, +0.014] | 未判定（換世界種子翻面） |
| 3（最後摸） | +0.045 (+0.045) | [−0.005, +0.100] | [−0.011, +0.096] | 未判定 |

**這裡更正步驟 3b 的一句話。** 3b 的「可以講的兩句話」寫「**行動家**單邊改吃 safety
可多賺 0.259 台（95% CI [+0.100, +0.555]）」，那個 CI 是 `EEEE` 的 **regret** 區間，
它在每次 resample 內部取四個角色的最大值，所以是「**某個**角色有賺」的區間，不能掛在
行動家頭上。3b 自己的角色偏離表就把角色 0 標成未判定（[+0.004, +0.555]，換種子翻面）。
新語料上行動家那格是 [−0.037, +0.355]，**跨零**——「行動家改防守有賺」這句話在 52 個
局面上沒有重現。

### 牌牆深度表（現在四格都讀得動）

| 深度 | 角色 | 局面數 | 收益 | 95% CI 世界種子1 | 世界種子2 | 判定 |
|---:|---:|---:|---:|---|---|---|
| 1 | 0 | 13 | +0.197 (+0.253) | [−0.105, +0.507] | [−0.058, +0.589] | 未判定 |
| 1 | 1 | 13 | +0.184 (+0.240) | [+0.003, +0.376] | [−0.008, +0.583] | 未判定（種子2翻面） |
| 1 | 2, 3 | 13 | 恰好 0 | — | — | 牌牆摸不到 |
| 2 | 0 | 13 | +0.090 (+0.062) | [−0.148, +0.312] | [−0.226, +0.282] | 未判定 |
| 2 | 1 | 13 | −0.075 (−0.141) | [−0.219, +0.077] | [−0.310, +0.031] | 未判定 |
| 2 | 2 | 13 | +0.047 (+0.028) | [−0.033, +0.126] | [−0.219, +0.235] | 未判定 |
| 2 | 3 | 13 | 恰好 0 | — | — | 牌牆摸不到 |
| 3 | 1 | 13 | −0.197 (−0.376) | [−0.367, −0.012] | [−0.619, −0.171] | 未判定（種子1翻面） |
| 3 | 2 | 13 | −0.147 (−0.196) | [−0.321, −0.025] | [−0.462, +0.005] | 未判定（世界種子2翻面） |
| 4 | 1 | 13 | −0.602 (−0.517) | [−0.911, −0.348] | [−0.827, −0.246] | **防守會虧** |
| 4 | 2 | 13 | −0.196 (−0.136) | [−0.410, −0.004] | [−0.295, +0.001] | 未判定（種子翻面） |
| 4 | 3 | 13 | +0.124 (+0.134) | [+0.004, +0.285] | [+0.015, +0.255] | **防守有賺** |

（深度 3、4 的角色 0 與其餘未列的格子全部未判定。）

兩個世界種子、八個 bootstrap 種子都不翻的只有三格：**全語料的角色 1 防守會虧**、
**深度 4 的角色 1 防守會虧 0.5–0.6 台**、**深度 4 的角色 3 防守賺 0.12–0.13 台**。
「位置相依」這個缺口仍然成立，而且方向跟步驟 3b 一致（下家不該防守、最後摸的該防守），
但步驟 3b 的深度 4 角色 1 是 −0.243、現在是 −0.52～−0.60，**數值不可直接比較**：語料
的手牌和深度組合都換了。

`_production_discard_policy` 是座位無關、深度無關的純進張規則，看不到這個特徵。

### 機制檢查

- **恰好零**：深度 1 的角色 2、3 和深度 2 的角色 3 偏離收益恰好為 0（有理數精確），
  由 `test_a_role_the_wall_never_reaches_gains_exactly_zero` 釘住。
- **零和殘差** 1.9e-16／2.2e-16。
- 快測全套 291 passed（含新的 7 個語料測試），19 個 slow 未跑；`tests/test_claims.py`
  未動。

### 這一步還是不能講什麼

- **沒有解出均衡**，「在殘局子賽局中求解到均衡」**仍然不能講**。
- 「行動家改防守有賺」**不能講**（52 個局面上跨零），步驟 3b 那句要照上面更正。
- 新語料的 8 個手牌是均勻抽樣的，不是實戰牌型分布。
- 角色與莊家的混淆照舊：莊家是絕對座位 0，每個角色仍混了 26 局莊、26 局閒。
- 抽象仍然只有兩個行為；子賽局 ≠ 完整牌局；不含鳴牌；不對真人。

### 解除了哪一條守門

**一條都沒有。** `gto`／`理論最佳`／`最佳解` 維持封鎖，`tests/test_claims.py` 未動。

### 還剩什麼

清單第 2 項做完了。它買到的是角色 1 的判定和一張讀得動的深度表，**沒有**買到均衡。

1. **改判準**：把「證明是均衡」換成「regret 的 95% 上界 ≤ ε」的 ε-均衡陳述。這是唯一
   能真的收斂的說法，而且不需要新預算——現在就能講 `SEES` 至多 0.07 台。
2. **把 `safety` 換成真的放銃機率估計**（原清單第 3 項）。現在的「未見剩餘為 0」很粗，
   而角色 1 防守穩定會虧這件事，有可能是這個粗近似造成的，不是真的策略性質。

---

## 步驟 3d（2026-08-26）：受限成兩玩家，**解出一個均衡**

104 個局面（26 個手牌樣板 × 4 個深度）＋把角色 2、3 固定成環境。

### 先講結論

**`SEEE` 是這個兩玩家子賽局的均衡，CI 完全落在 0 以下，兩個世界種子、八個 bootstrap
種子全部一致。** 這是本專案第一個依照既定判準真正**證出來**的均衡。

| 世界種子 | regret | 95% CI | 判定 |
|---:|---:|---|---|
| 1 | −0.1461 | **[−0.217, −0.041]** | **是均衡** |
| 2 | −0.1799 | **[−0.236, −0.018]** | **是均衡** |

`SEEE` ＝ 行動家吃 safety、下家吃 efficiency，角色 2、3 維持 production 現行打法。

### 為什麼四玩家證不出來、兩玩家證得出來

不是預算問題，是 **regret 的定義**問題。regret ＝「所有**可偏離**角色裡最大的偏離
收益」。取最大值的候選越多，每次 bootstrap resample 算出的最大值就越高，整個區間就
被往上推。所以：

- 可偏離角色**越多** → 區間越高 → 越容易「排除」，越難「證明」
- 可偏離角色**越少** → 區間越低 → 越難「排除」，越容易「證明」

104 局面上同一個 `EEEE` 的對照（世界種子 1）：四玩家 [+0.068, +0.401]，
兩玩家 [+0.046, +0.401]。**上界一模一樣，下界掉下來**——差別完全來自取最大值的候選
數，不是來自資料。

四玩家設定天生偏向排除，這正是它在 52 局面排除掉 12 個卻證不出任何一個的原因。要
「證明」就必須減少可偏離的角色。這不是放寬判準，判準一字未改；換的是**問題**。

### 代價：兩張表回答的是不同問題，不可互推

`SEEE` 在**四玩家**表裡是**被排除的**（[+0.035, +0.123]，不是均衡），因為角色 2 或 3
改打法有賺。所以：

> **可以講**：在角色 2、3 維持 production 現行打法的前提下，行動家改吃 safety、下家
> 維持 efficiency，是這個兩玩家子賽局的均衡（regret −0.15／−0.18 台，
> 95% CI [−0.217, −0.041]／[−0.236, −0.018]，104 局面 × 100 世界，重抽局面）。

> **不可以講**：`SEEE` 是四家的均衡。它在四玩家表裡明確被排除。

兩張表必須分開陳述，任一方的結論都不能轉移到另一方。`scripts/empirical_game_table.py`
一次建表同時輸出兩張，並在開頭斷言「四個角色全開的受限 regret」與 `regret_interval()`
逐位元相同，不同就中止——這是防止兩張表用了不同統計程序的唯一保險。

### 四玩家表在 104 局面上的變化：從 12 個排除變成 14 個

| 世界種子 | 被穩健排除 | 未判定 |
|---:|---:|---|
| 1 | 14 | `SEES`、`SESS` |
| 2 | 14 | `SEES`、`SESS` |

52 局面時未判定的是 `EEES`／`SEES`／`SESS`／`SSES` 四個，加倍後 `EEES` 和 `SSES` 都被
排除，剩下的兩個**兩個世界種子完全一致**（52 局面時剩哪四個還會隨種子變）。

`SEES`／`SESS` 的 regret 是 ∓0.016／∓0.023 台，兩個種子連正負號都相反，CI 半寬 0.045。
**這兩格用現有判準永遠證不出來**：真值離 0 比量測精度還近，要讓半寬縮到比 0.016 還小
需要約 800 個局面（104 × (0.045/0.016)² ≈ 823），而如果真值就是 0（弱均衡），再多局面
也不會讓上界掉到 0 以下。這正是步驟 3c「判準只能排除、不能證明弱均衡」那段講的情況，
`SEEE` 之所以證得出來，是因為它的真值離 0 有 0.15 台的距離。

### 角色偏離表（104 局面，從全 efficiency 出發）

| 角色 | 收益 種子1 (種子2) | 95% CI 種子1 | 種子2 | 判定 |
|---|---:|---|---|---|
| 0（行動家） | +0.222 (+0.180) | [+0.046, +0.401] | [+0.019, +0.349] | **防守有賺** |
| 1（下家） | −0.196 (−0.207) | [−0.304, −0.095] | [−0.303, −0.111] | **防守會虧** |
| 2 | −0.062 (−0.057) | [−0.124, +0.002] | [−0.126, +0.016] | 未判定 |
| 3（最後摸） | +0.066 (+0.050) | [+0.022, +0.113] | [+0.001, +0.103] | 未判定（種子2翻面） |

**這裡再更正一次步驟 3c 的一句話。** 3c 說「行動家改防守有賺這句話在 52 個局面上沒有
重現」（當時是 [−0.037, +0.355]，跨零）。**在 104 個局面上它重現了**，兩個世界種子、
八個 bootstrap 種子都不翻面：**行動家單邊改吃 safety 賺 0.18–0.22 台**。所以步驟 3b
那句原始結論的**方向是對的**，錯的是它掛在 regret 區間上；用不做選擇的角色偏離區間、
在 104 個局面上，這句話成立。

現在全語料層級站得住的有三句：行動家防守有賺、下家防守會虧、（深度 4）角色 2 防守會虧。

### 牌牆深度表（104 局面，每格 26 個局面）

兩個世界種子都不翻面的格子：

| 深度 | 角色 | 收益 種子1 (種子2) | 判定 |
|---:|---:|---:|---|
| 1 | 1 | +0.197 (+0.182) | **防守有賺** |
| 3 | 1 | −0.246 (−0.358) | **防守會虧** |
| 4 | 1 | −0.679 (−0.549) | **防守會虧** |
| 4 | 2 | −0.211 (−0.133) | **防守會虧** |

**下家（角色 1）的防守收益隨牌牆深度變號**：牌牆只剩 1 張時防守賺 0.18–0.20 台，
剩 3–4 張時防守虧 0.25–0.68 台。這是目前最強的「位置×深度相依」證據，而
`_production_discard_policy` 是座位無關、深度無關的純進張規則，兩個維度都看不到。

深度 1 的角色 2、3 與深度 2 的角色 3 偏離收益**恰好為 0**（有理數精確），因為牌牆摸
不到它們。

### 機制檢查

- 零和殘差 2.8e-16／2.2e-16。
- `restricted_regret` 在四個角色全開時與 `regret_interval()` 逐位元相同（腳本內斷言）。
- 快測全套 293 passed（含 9 個語料測試），19 slow 未跑；`tests/test_claims.py` 未動。
- 52 局面語料仍完整可重現：`empirical_game_cases(13)` 與步驟 3c 用的逐個局面相同
  （名稱、seed、state），由 `test_the_52_case_corpus_stays_reproducible` 釘住。

### 這一步還是不能講什麼

- **不能講四家的均衡**。`SEEE` 只是兩玩家受限賽局的均衡，四玩家表裡它被排除。
- `SEES`／`SESS` 在四玩家表裡仍然未判定，且用現有判準可能永遠無法判定。
- 語料的 21 個手牌是均勻抽樣的，不是實戰牌型分布。
- 角色與莊家的混淆照舊；抽象仍只有兩個行為；子賽局 ≠ 完整牌局；不含鳴牌；不對真人。

### 解除了哪一條守門

**一條都沒有。** `gto`／`理論最佳`／`最佳解` 維持封鎖，`tests/test_claims.py` 未動。
解除守門要先決定「兩玩家受限子賽局的均衡」算不算數，那是措辭範圍的判斷，不是量測結果。

### 還剩什麼

1. **把 `safety` 換成真的放銃機率估計**。「下家防守會虧、但牌牆剩 1 張時防守有賺」這個
   變號很可能是「未見剩餘為 0」這個粗近似造成的，不是真的策略性質。
2. **精確世界集合**：把「抽 100 個世界」換成「k 個最可能世界＋精確機率權重」，payoff
   變成精確有理數，完全不需要 CI。這是唯一能回答 `SEES`／`SESS` 那兩格的路。

---

## 步驟 3e（2026-08-26）：換掉 `safety`，賽局收斂到**唯一一個**候選均衡

清單第 1 項做完了。`taimahjong/empirical_game.py` 新增 `deal_in_risk`，語料與世界預算
不變（104 局面 × 100 世界，兩個世界種子）。下表 `D` 指 `deal_in_risk`。

### `safety` 錯在哪

`safety` 只讀一個位元：這張牌還有沒有未見的副本？它的理由是「沒有未見副本的牌，不會
被需要**第二張同牌**的手等到」——這對單騎、雙碰成立，**對兩面聽不成立**。兩面聽要的是
**鄰牌**，不是這張牌本身：對手持 3p4p 就是在聽 2p 和 5p，不管 5p 已經現了幾張。

`deal_in_risk` 改成走 `danger._wait_shapes` 的標準聽型，每一型用「對手手上握有該型所需
牌」的機率加權。構造案例：

| 牌 | 未見副本 | 鄰牌 | 危險度 |
|---|---:|---|---:|
| 5p | **0** | 全活 | **10.0** |
| 5m | 1 | 全死 | 0.8 |

`safety` 判定前者絕對安全，`deal_in_risk` 判定它比後者危險 12 倍。語料上兩者 **60% 的
決策不同**，是不同行為而非改名。由
`test_a_tile_with_no_unseen_copies_is_not_automatically_safe` 釘住。

兩個近似要一起記，都是資訊限制逼出來的：座位只看得到自己的手牌與未見池，所以對手的手
被當成從該池抽出，且每張所需牌**視為獨立抽取**（只在池夠大時才對）。

### 主結果：16 個 profile 只剩 1 個未判定

| 世界種子 | 被穩健排除 | 未判定 |
|---:|---:|---|
| 1 | **15** | `DDDD` |
| 2 | **15** | `DDDD` |

對照 `safety` 版的 14 個排除／2 個未判定（`SEES`、`SESS`）。**換一個更好的防守策略，
四玩家賽局就收斂到唯一一個候選均衡** `DDDD`（四家都用 `deal_in_risk`）：

| 世界種子 | regret | 95% CI | 判定 |
|---:|---:|---|---|
| 1 | −0.0096 | [−0.020, +0.006] | 未判定 |
| 2 | −0.0096 | [−0.025, +0.014] | 未判定 |

**點估計在兩個世界種子下完全相同（−0.0096），而且是負的**，這跟 `safety` 版剩下的兩格
（`SEES`／`SESS` 連正負號都隨種子相反）是不同性質的「未判定」。CI 半寬 0.013／0.020，
要把上界壓到 0 以下需要把半寬縮到 0.0096 以下，即約 **190–430 個局面**（現在 104）。
這是本專案第一次出現「加預算就能判定」而不是「可能永遠判不了」的四玩家格子。

**但仍然只是候選，不准講成解出來。** 判準未變，CI 跨零就是未判定。

### production 對更好的防守模型更容易被打

角色偏離表（從全 efficiency 出發改吃 `deal_in_risk`）：

| 角色 | 收益 種子1 (種子2) | 95% CI 種子1 | 種子2 | 判定 |
|---|---:|---|---|---|
| 0（行動家） | +0.323 (+0.262) | [+0.138, +0.516] | [+0.087, +0.446] | **有賺** |
| 1（下家） | −0.054 (−0.046) | [−0.161, +0.051] | [−0.142, +0.052] | 未判定 |
| 2 | +0.071 (+0.081) | [+0.020, +0.127] | [+0.017, +0.147] | **有賺** |
| 3（最後摸） | +0.118 (+0.122) | [+0.078, +0.165] | [+0.083, +0.167] | **有賺** |

跟 `safety` 版比，**四個角色裡有三個從「未判定或會虧」變成「防守有賺」**，行動家的收益
也從 +0.18～+0.22 抬到 +0.26～+0.32 台。也就是說：production 的全進張打法，面對一個
**像樣的**防守模型時比先前量到的更容易被利用；先前的 exploitability 被 `safety` 的粗糙
低估了。

### 步驟 3d 那個「下家防守會虧」的結論，一半是 `safety` 造成的

3d 在 `safety` 下量到「下家防守會虧 0.20 台」（全語料，穩健）。換成 `deal_in_risk` 後
全語料層級變成 **−0.05 台、跨零、未判定**。深度表：

| 深度 | 角色 1 用 `safety` | 角色 1 用 `deal_in_risk` |
|---:|---:|---:|
| 1 | +0.197 / +0.182（有賺） | +0.316 / +0.364（有賺） |
| 3 | −0.246 / −0.358（會虧） | −0.072 / −0.178（未判定／會虧） |
| 4 | −0.679 / −0.549（會虧） | −0.545 / −0.414（會虧） |

**方向沒變、幅度縮小、全語料的判定消失。** 所以 3d 寫的「下家防守會虧」是**部分成立**：
牌牆 4 張時用哪個防守模型都會虧（−0.41～−0.68），但全語料層級那句在較好的模型下站不
住。3d 懷疑「這個結論可能是粗近似造成的」，量下來是**對了一半**。

**牌牆深度的變號仍然成立**，而且在更好的模型下更強：牌牆剩 1 張時下家防守賺
0.32–0.36 台，剩 4 張時虧 0.41–0.54 台。`_production_discard_policy` 座位無關、深度無關，
這個缺口不受防守模型選擇影響。

### 兩玩家表（角色 2、3 固定）

**`DEEE` 在 `deal_in_risk` 下不再是均衡**：[−0.137, +0.021]／[−0.151, +0.018]，兩個種子
都跨零、未判定。3d 那個證出來的均衡是 **`safety` 專屬的**，換防守模型就沒了。

這件事本身是重要的：**「解出的均衡」對抽象裡策略的定義有依賴性**，而抽象是人選的。
3d 的 `SEEE` 結論仍然成立（它明確帶著 `{efficiency, safety}` 這個抽象），但它不能被讀成
「行動家該防守」這種與抽象無關的話。

### 機制檢查

- 零和殘差 2.8e-16（兩個種子）。
- `restricted_regret` 在四角色全開時與 `regret_interval()` 逐位元相同（腳本內斷言）。
- 快測全套 295 passed，19 slow 未跑；`tests/test_claims.py` 未動。

### 這一步還是不能講什麼

- **沒有解出均衡**。`DDDD` 是候選，CI 跨零。
- 3d 的 `SEEE` 均衡**不能**跨抽象轉述；它只在 `{efficiency, safety}` 裡成立。
- `deal_in_risk` 的兩個獨立性近似照舊；抽象仍只有兩個行為；子賽局 ≠ 完整牌局；不含
  鳴牌；不對真人。

### 解除了哪一條守門

**一條都沒有。** `gto`／`理論最佳`／`最佳解` 維持封鎖，`tests/test_claims.py` 未動。

### 還剩什麼

1. **把語料加到約 200–450 個局面**，判定 `DDDD`。這是第一次有明確的預算數字對應到一個
   四玩家格子的判定，而不是「可能永遠判不了」。
2. **精確世界集合**（原第 2 項）：payoff 變精確有理數，完全不需要 CI。
