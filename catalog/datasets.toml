catalog_version = 1
as_of = "2026-08-31"

[[datasets]]
id = "production_calibration"
layer = "curated_reference"
path = "data/calibration.json"
optional = false
format = "json"
owner = "scripts/generate_calibration.py"
regeneration_command = "python scripts/generate_calibration.py --games 2000 --seed-start 40001 --workers 4 --verify-games 12 --out data/calibration.json"
primary_key = "tenpai: melds|turn_bucket|run_bucket; deal_in: danger bucket; fold: policy"
schema = ["version", "metadata", "counts.games", "counts.tenpai", "counts.deal_in", "counts.fold", "tables.tenpai", "tables.deal_in", "tables.fold", "quality"]
size_bytes = 16341
update_frequency = "manual promotion; no schedule"
reproducible_from_recorded_seed = false
known_gap = "Seeds, sample sizes, and producer commit are recorded, but generation consumed the previous data/calibration.json identified only by a SHA-256; those input bytes were overwritten and are not preserved."

[[datasets]]
id = "independent_calibration_audit"
layer = "curated_reference"
path = "data/calibration-independent.json"
optional = false
format = "json"
owner = "scripts/generate_calibration.py"
regeneration_command = "python scripts/generate_calibration.py --games 8000 --seed-start 50001 --workers 4 --verify-games 12 --independent-policy --out data/calibration-independent.json"
primary_key = "calibration cells as production_calibration; audit sections keyed by comparison and tail band"
schema = ["version", "metadata", "counts", "tables", "quality", "audit.shipped_table", "audit.candidate_table", "audit.tail"]
size_bytes = 28733
update_frequency = "one-off audit candidate; no schedule"
reproducible_from_recorded_seed = true

[[datasets]]
id = "exact_reference_corpus"
layer = "curated_reference"
path = "taimahjong/reference_ev.py"
optional = false
format = "python-defined records"
owner = "taimahjong.reference_ev.representative_reference_cases"
regeneration_command = "not applicable: the 26 records are deterministically defined in the module"
primary_key = "ReferenceCase.name"
schema = ["name", "seed", "state.players", "state.wall", "state.acting_seat", "state.next_seat", "state.dealer_streak", "state.scheme", "strata"]
size_bytes = 34333
update_frequency = "manual, test-gated"
reproducible_from_recorded_seed = true

[[datasets]]
id = "ev_benchmark_corpus"
layer = "curated_reference"
path = "taimahjong/ev_benchmark.py"
optional = false
format = "python-defined records"
owner = "taimahjong.ev_benchmark.benchmark_corpus"
regeneration_command = "not applicable: the 6 records are deterministically defined in the module"
primary_key = "EVBenchmarkCase.name"
schema = ["name", "hand", "opponents", "visible", "turns", "seed", "scheme", "tags"]
size_bytes = 6639
update_frequency = "manual, test-gated"
reproducible_from_recorded_seed = true

[[datasets]]
id = "empirical_game_corpus"
layer = "curated_reference"
path = "taimahjong/empirical_corpus.py"
optional = false
format = "python-defined records"
owner = "taimahjong.empirical_corpus.empirical_game_cases"
regeneration_command = "not applicable: 104 templates x 4 wall depths are deterministically generated by the module"
primary_key = "ReferenceCase.name"
schema = ["name", "seed", "state", "strata"]
size_bytes = 21702
update_frequency = "manual, prefix-reproducibility test-gated"
reproducible_from_recorded_seed = true

[[datasets]]
id = "strategy_experiment_results"
layer = "report_artifact"
path = "docs/experiments.md"
optional = false
format = "markdown tables"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "study + condition/policy"
schema = ["study", "condition", "games", "seed_base", "point_estimate", "paired_interval_or_gap"]
size_bytes = 9888
update_frequency = "historical one-off report"
reproducible_from_recorded_seed = false
known_gap = "The document lists component commands and seeds, but no command assembles the complete Markdown artifact and the original paired second moments were not saved."

[[datasets]]
id = "batch_a_before_after_results"
layer = "report_artifact"
path = "docs/batch-a-simulation-report.md"
optional = false
format = "markdown table"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "Case"
schema = ["Case", "Shanten", "Seed", "Hand", "Top before_to_after", "P(win) before_to_after", "Runtime seconds before_to_after"]
size_bytes = 3699
update_frequency = "one-off batch report"
reproducible_from_recorded_seed = false
known_gap = "The report records 30 positions, seeds, and 32 trials per candidate, but not the producer command or code commit."

[[datasets]]
id = "calibration_audit_report"
layer = "report_artifact"
path = "docs/calibration-audit.md"
optional = false
format = "markdown tables"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "audit section + bucket/sample size"
schema = ["fit/holdout counts", "quality metrics", "reliability buckets", "tail bands", "bootstrap interval"]
size_bytes = 8728
update_frequency = "one-off audit report"
reproducible_from_recorded_seed = false
known_gap = "The JSON regeneration command is recorded, but no formatter or exact command regenerates the Markdown report; its stated 5,000 bootstrap replicates also differ from the JSON's 2,000."

[[datasets]]
id = "ev_reference_report"
layer = "report_artifact"
path = "docs/ev-reference-report.md"
optional = false
format = "markdown tables"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "metric or simulation budget"
schema = ["cases", "candidate comparisons", "MAE", "top-1 agreement", "ranking inversions", "regret", "rank correlation", "timing"]
size_bytes = 3684
update_frequency = "manual after reference-corpus or EV changes"
reproducible_from_recorded_seed = false
known_gap = "python scripts/ev_reference_report.py --sims 24 recomputes JSON metrics, but no command regenerates the Markdown artifact and no code commit is embedded."

[[datasets]]
id = "ev_replacement_assessment_results"
layer = "report_artifact"
path = "docs/ev-replacement-assessment.md"
optional = false
format = "markdown table"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "metric"
schema = ["Exact cases", "Candidate comparisons", "Mean absolute actor-EV error", "Top-1 agreement", "Non-tied ranking pairs", "Ranking inversion rate"]
size_bytes = 5311
update_frequency = "historical decision record"
reproducible_from_recorded_seed = false
known_gap = "The recorded 2-case result predates the current 26-case corpus; no producing commit or artifact-generation command is recorded."

[[datasets]]
id = "hidden_world_strata_results"
layer = "report_artifact"
path = "docs/hidden-world-strata.md"
optional = false
format = "markdown tables"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "study date + simulation budget + pool + stratification + repeat/case"
schema = ["pool", "stratification", "sims", "repeat", "CI widths", "resolved count", "wall time", "peak memory"]
size_bytes = 18228
update_frequency = "manual study follow-up"
reproducible_from_recorded_seed = false
known_gap = "The probe and parameters are described, but the complete command matrix, producer commits, and raw per-run JSON were not preserved with the report."

[[datasets]]
id = "equilibrium_study_results"
layer = "report_artifact"
path = "docs/equilibrium-plan.md"
optional = false
format = "markdown tables and decision log"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "dated study step + profile/role/depth"
schema = ["profile", "regret", "95% CI", "verdict", "seed robustness", "role", "wall depth", "payoff"]
size_bytes = 66818
update_frequency = "append-only research phases"
reproducible_from_recorded_seed = false
known_gap = "Many phases record budgets and seeds, but the file is a hand-maintained composite with no single exact regeneration command or per-phase code commits."

[[datasets]]
id = "full_review_measurements"
layer = "report_artifact"
path = "docs/codex-gpt-5.6-sol-full-review.md"
optional = false
format = "markdown tables and captured text"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "review phase + check/measurement"
schema = ["check", "command or method", "result", "seed", "sample budget", "runtime", "memory"]
size_bytes = 38833
update_frequency = "one-off repository review"
reproducible_from_recorded_seed = false
known_gap = "Commands and selected seeds are present, but the report does not identify one producing commit for every measurement or provide an artifact builder."

[[datasets]]
id = "engine_teardown_measurements"
layer = "report_artifact"
path = "docs/teardown.md"
optional = false
format = "markdown tables"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "section + measured seed/configuration"
schema = ["pipeline stage", "configuration", "seed", "top discard", "EV gap", "interpretation"]
size_bytes = 20267
update_frequency = "one-off explanatory report"
reproducible_from_recorded_seed = false
known_gap = "The narrative records selected measurements but not a complete command, sample budget, and code commit for every result table."

[[datasets]]
id = "readme_screenshot_evidence"
layer = "report_artifact"
path = "docs/screenshots"
optional = false
format = "png bundle"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "relative file path"
schema = ["analyze-ev-ranking.png", "trainer-feedback.png"]
size_bytes = 570627
minimum_file_count = 2
bundle_extensions = [".png"]
required_members = ["analyze-ev-ranking.png", "trainer-feedback.png"]
update_frequency = "manual UI documentation update"
reproducible_from_recorded_seed = false
known_gap = "No capture command, viewport metadata, application commit, or seed manifest is stored with these two images."

[[datasets]]
id = "ui_baseline_before"
layer = "report_artifact"
path = "docs/ui-baseline/before"
optional = false
format = "png bundle with json manifests"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "screen label + viewport"
schema = ["label", "blockedLandscape", "wanted", "wantedPresent", "pageErrors", "PNG pixels"]
size_bytes = 8075661
expected_file_count = 65
bundle_extensions = [".png", ".json"]
required_members = ["capture-report.json", "capture-report-evscreens.json"]
update_frequency = "one-off pre-redesign baseline"
reproducible_from_recorded_seed = false
known_gap = "The design plan contains a Playwright code fragment and fixed Math.random override, but the runnable harness and exact command are absent from the repository."

[[datasets]]
id = "ui_pre_evfix_evidence"
layer = "report_artifact"
path = "docs/ui-baseline/pre-evfix-evidence"
optional = false
format = "png bundle with json manifest"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "screen label + viewport"
schema = ["label", "blockedLandscape", "wanted", "wantedPresent", "pageErrors", "PNG pixels"]
size_bytes = 2033848
expected_file_count = 16
bundle_extensions = [".png", ".json"]
required_members = ["capture-report.json"]
update_frequency = "one-off pre-fix evidence"
reproducible_from_recorded_seed = false
known_gap = "No runnable capture harness, exact command, application commit, or complete seed/configuration record is committed."

[[datasets]]
id = "tile_face_vector_data"
layer = "curated_reference"
path = "server/static/js/tile-faces.js"
optional = false
format = "javascript data module"
owner = "unknown"
regeneration_command = "unknown"
primary_key = "engine tile index 0-33 and named layout/token"
schema = ["FACE_TOKENS", "NUMERAL_FACES", "HONOR_FACES", "DOT_LAYOUTS", "BAMBOO_LAYOUTS", "tileFaceSvg", "tileBackSvg"]
size_bytes = 5960
update_frequency = "manual UI asset change"
reproducible_from_recorded_seed = false
known_gap = "scripts/gen_tile_faces.py names this path but emits TILE_FACE_FONT/TILE_GLYPHS instead of the exports current readers require; it is not a valid producer for the committed module."
